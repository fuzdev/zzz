// The one text glyph with no SVG-icon replacement from `@fuzdev/fuz_ui/icons.js`:
// `GLYPH_PLACEHOLDER` prefixes input `placeholder` text — an attribute can't host an SVG.
// Everything else renders an `icon_*` export via `Svg`; action/part icon lookups
// live in `action_icons.ts` and `part_helpers.ts`.

export const GLYPH_PLACEHOLDER = '↳';
