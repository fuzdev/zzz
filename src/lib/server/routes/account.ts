/**
 * zzz session configuration.
 *
 * @module
 */

import {create_session_config} from '@fuzdev/fuz_app/auth/session_cookie.js';

export const zzz_session_config = create_session_config('zzz_session');
